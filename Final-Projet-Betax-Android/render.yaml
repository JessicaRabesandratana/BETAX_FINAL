services:
  - type: web
    name: betax-chat-proxy
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: NODE_VERSION
        value: "18"
